const dbConfig = {
    host: 'localhost',
    user: 'root',
    password: 'Sanuth123',  // Fresh XAMPP installation has no password by default
    database: 'phishing_detector',
    port: 3306
};

module.exports = dbConfig;